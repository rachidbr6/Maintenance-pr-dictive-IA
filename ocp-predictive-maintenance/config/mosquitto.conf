listener 1883
persistence true
persistence_location /mosquitto/data/

# Developpement local uniquement. En atelier : TLS + authentification par ACL,
# un compte par passerelle, droits d'ecriture restreints a son propre prefixe
# de topic.
allow_anonymous true

max_queued_messages 10000
